spring.application.name=finApp

# Configuración de la base de datos
spring.datasource.url=jdbc:postgresql://localhost:5432/finapp
spring.datasource.username=postgres
spring.datasource.password=penaldo10
spring.datasource.driver-class-name=org.postgresql.Driver

# Configuración de JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Configuración de JWT
finapp.app.jwtSecret=finAppSecretKey1234567890ABCDEFGHIJKLMNOPQRSTUVWXYZ
finapp.app.jwtExpirationMs=86400000

# Configuración del servidor (opcional si quieres especificar un puerto)
server.port=8080